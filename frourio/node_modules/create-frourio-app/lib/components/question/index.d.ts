import { FC } from 'react';
import { Answers, DeterminedPrompt } from '~/server/common/prompts';
export interface QuestionProps {
    answers: Answers;
    question: DeterminedPrompt;
    onChoice?: (name: keyof Answers, value: string) => void;
    onTouch?: () => void;
    touched: boolean;
    addInfo?: unknown;
    addError?: unknown;
}
export interface QuestinoInputProps extends QuestionProps {
    question: DeterminedPrompt & {
        type: 'input';
    };
}
export interface QuestinoListProps extends QuestionProps {
    question: DeterminedPrompt & {
        type: 'list';
    };
}
declare const Question: FC<QuestionProps>;
export default Question;
