import { FC, MouseEventHandler } from 'react';
export interface PrimaryButtonProps {
    onClick?: MouseEventHandler;
    disabled?: boolean;
    children: React.ReactNode;
}
declare const PrimaryButton: FC<PrimaryButtonProps>;
export default PrimaryButton;
